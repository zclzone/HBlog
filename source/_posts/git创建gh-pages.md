---
title: 插入排序算法
date: 2020-08-30 08:30:07
top: false
categories: git
tags: git
---

```shell
  cd dist（dist为文件夹名）

  git init

  git checkout --orphan gh-pages（创建并切换分支）

  git add .

  git commit -m 'xxx'

  git remote add origin xxxx.git

  git push -u origin gh-pages（指定推送到gh-pages分支）

```
